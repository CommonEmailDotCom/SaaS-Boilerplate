name: Checkly

# Disabled - requires CHECKLY_API_KEY and CHECKLY_ACCOUNT_ID secrets
on:
  workflow_dispatch:

jobs:
  test-e2e:
    name: Test E2E on Checkly
    runs-on: ubuntu-latest
    steps:
      - run: echo "Run manually when Checkly is configured"
