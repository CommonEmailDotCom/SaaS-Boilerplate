name: Crowdin Action

# Disabled - requires CROWDIN_PROJECT_ID and CROWDIN_PERSONAL_TOKEN secrets
on:
  workflow_dispatch:

jobs:
  synchronize-with-crowdin:
    name: Synchronize with Crowdin
    runs-on: ubuntu-latest
    steps:
      - run: echo "Run manually when Crowdin is configured"
